/**
 * DELETED — replaced by `src/ui/week-view.ts` (docs/WEEK-VIEW.md).
 *
 * This file used to render "My classes": five course rows, grouped under whichever
 * weekday the course first met. It was the wrong shape for the question the app
 * exists to answer. A course that meets Tue/Wed/Fri appeared once, under Tuesday, so
 * the list could not show the *shape* of a week — that Monday starts at 08:30 and has
 * a six-hour hole in it, that Wednesday is the heavy day, that two Monday classes are
 * in the same room. The week view shows every session on every day, with the gaps
 * between them as rows of their own.
 *
 * Nothing imports this module. The file is kept as a tombstone so the deletion is
 * legible in the tree rather than silent; it may be removed outright.
 */
export {}
